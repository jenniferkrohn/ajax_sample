<%= form_for @task, remote: true do |f| %>
  <%= f.text_area :description, placeholder: "Describe the task" %> <br/>
  <%= f.text_field :deadline, id: "datetimepicker1", placeholder: "When is it due?" %> 
  <%= f.button :submit, class: "submit" %> 
<% end %>